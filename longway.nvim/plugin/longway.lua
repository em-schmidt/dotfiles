-- Auto-loaded plugin file for longway.nvim
-- This file is sourced automatically by Neovim

if vim.g.loaded_longway then
  return
end
vim.g.loaded_longway = true

-- The plugin is lazy-loaded, actual setup happens when user calls require('longway').setup()
